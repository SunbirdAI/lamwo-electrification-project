import { Component, ReactElement } from 'react';
import { ArcData } from '../PieChart';
import { PieArc, PieArcProps } from './PieArc';
import { PieArcLabel, PieArcLabelProps } from './PieArcLabel';
import { ColorSchemeType } from '../../common/color';
export interface PieArcSeriesProps {
    /**
     * Animated set by the `PieArc` components.
     */
    animated: boolean;
    /**
     * Outer radius set by the parent component.
     */
    outerRadius: number;
    /**
     * Inner radius set by the parent component.
     */
    innerRadius: number;
    /**
     * Pad Angle between adjacent arcs, see https://github.com/d3/d3-shape#arc_padAngle
     */
    padAngle: number;
    /**
     * Pad Radius between adjacent arcs, see https://github.com/d3/d3-shape#arc_padRadius
     */
    padRadius: number;
    /**
     * Corner Radius of the arcs, see https://github.com/d3/d3-shape#arc_cornerRadius
     */
    cornerRadius: number;
    /**
     * Data set by the parent component.
     */
    data: ArcData[];
    /**
     * Width of the arc
     */
    arcWidth: number;
    /**
     * Doughnut, render as a donut shape
     */
    doughnut: boolean;
    /**
     * Explode: OuterRadius will be adjusted by the data property
     */
    explode: boolean;
    /**
     * Display all labels shows labels even if there is little space
     */
    displayAllLabels: boolean;
    /**
     * Label component
     */
    label?: ReactElement<PieArcLabelProps, typeof PieArcLabel> | null;
    /**
     * Arc Component
     */
    arc: ReactElement<PieArcProps, typeof PieArc>;
    /**
     * Color scheme
     */
    colorScheme: ColorSchemeType;
    /**
     * Height set by the parent component
     */
    height: number;
    /**
     * Width set by the parent component
     */
    width: number;
}
export declare class PieArcSeries extends Component<PieArcSeriesProps> {
    static defaultProps: Partial<PieArcSeriesProps>;
    calculateRadius(): {
        outerRadius: number;
        innerRadius: number;
        labelWidth: number;
    };
    shouldDisplayLabel(arcData: ArcData): boolean;
    /**
     * @param data
     * @param outerRadius - an outerRadius of the pie
     * @param minDistance - minimal vertical distance between adjacent labels
     */
    calculateLabelPositions: (data: ArcData[], outerRadius: number, minDistance: number, cornerRadius: number, padAngle: number, padRadius: number) => Array<[number, number] | null>;
    innerArc(innerRadius: number, outerRadius: number, cornerRadius: number, padAngle: number, padRadius: any): (point: ArcData) => string;
    calculateOuterRadius(outerRadius: number, point: ArcData): number;
    centroid(innerRadius: number, outerRadius: number): (data: ArcData) => [number, number];
    render(): JSX.Element;
}
